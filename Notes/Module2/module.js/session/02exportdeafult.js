// This function takes two arguments, x and y, and logs their sum to the console
function sum(x, y) {
   console.log(x + y);
}

// 'export default' is used to export a single function, class, or value as the default export
// This allows the function to be imported with any name in another file
export default sum;


/// check index.js for its import & usage