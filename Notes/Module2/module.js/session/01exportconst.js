// 'export const' is used to export a function, variable, or constant 
// so it can be imported and used in another file.

// This function calculates the difference between two numbers (x and y)
// It logs the result in a formatted string to the console
export const sub = (x, y) => {
    console.log(`The difference of two numbers are ${x - y}`);
};


/// check index.js for its import & usage